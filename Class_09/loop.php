<?php 

	$Start = 100;
	$Limit = 0;
	$Step = 1;

	// for loop
	// while loop
	// do while loop
	// foreach loop

	for($Counter = $Start; $Counter >= $Limit; $Counter -= $Step)
	{
		echo $Counter. "<br>";
	}